# BootCamp

This repository contains a minimal discrete-event simulation demo for a can-line.

## Quick start

Run a sample scenario:

```bash
python -m sim.apps.run_line --scenario sim/scenarios/demo_line.yaml
```

Run the tests:

```bash
pytest -q
```
